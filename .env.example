# Google AI Studio에서 발급받은 Gemini API 키 (필수)
# https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# SQLite 파일 경로 (선택, 기본값: database.db)
# 프로젝트 루트가 아닌 곳에서 실행하면 빈 DB가 새로 생기므로 필요할 때만 지정한다.
# DB_PATH=database.db
